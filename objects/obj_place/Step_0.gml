/// @description Tile placement code

for(j = 0; j < obj_grid.y_limit + 1; j++) // Loops through and draws X numbers
{	
	for(i = 0; i < obj_grid.x_limit + 1; i++) // Loops through and draws X numbers
	{
		//tilemap_get()
		//draw_sprite_ext(current_sprite, 1, j, i)
	}
}

if mouse_check_button(global.controls[0])
{
	
}

/************************************************************************
									DEBUG								*
*************************************************************************/

if keyboard_check_pressed(vk_space)
{
	//lay_id = layer_get_id("lyr_collide");
	//tile_id = layer_tilemap_get_id(lay_id);
	//tilemap_tileset(tile_id, tl_city);/
	//tilemap_set("lyr_collide", 2, 0, 0);
	
}